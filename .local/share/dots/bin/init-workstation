#!/usr/bin/env zsh

dots_bin=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
dots_includes=$(dirname $dots_bin)/include

for f in `ls $dots_includes`; do
    source $dots_includes/$f
done


    

